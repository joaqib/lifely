= simple_form_for [goal, task], remote: true do |f|
  = f.input :name, placeholder: "New Task", label: false
  = f.input :hours_planned, in: 0.5..8, step: 0.5, as: :decimal, label: false
  = f.button(:submit, "Save")