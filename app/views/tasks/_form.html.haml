= form_for [goal, task], remote: true do |f|
  = f.text_field :name, placeholder: "New Task"
  = f.number_field :hours_planned, in: 0.5..8, step: 0.5
  = f.button  class: "btn grey" do
    %i.fa.fa-plus