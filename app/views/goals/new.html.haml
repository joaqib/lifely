- if @goal.errors.any?
  %ul
    - @goal.errors.full_messages.each do |error_msg|
      %li= error_msg
.row
  .card.four.columns
    = form_for @goal do |f|
      = f.label :name
      = f.text_field :name
      = f.submit 'Create new Goal'
    
